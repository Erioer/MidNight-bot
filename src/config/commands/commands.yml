# ============================================================
# MidNight Command Policy
# ============================================================
# Controls every command in the bot from one file.
# Settings cascade:  defaults  ->  category  ->  command
# Only list what you want to change. Everything defaults to on.
#
# Key meanings:
#   isEnabled      – Whether the command/category loads at all.
#                    false removes it from slash, prefix, and the
#                    /commands dashboard entirely.
#   isSlashEnabled – Register as a slash command.
#   isPrefixEnabled – Usable via the text prefix (e.g. $help).
#   isAdminOnly    – Require the Manage Server permission.
#
# For commands that are structurally prefix-only (e.g. starboard,
# dogfact, fact, react) the isSlashEnabled key is ignored.
# For commands that are structurally slash-only (e.g. help,
# configwizard, play, queue, greet, wipedata) the isPrefixEnabled
# key is ignored.
#
# Restart the bot to apply changes. Slash-registration changes
# can take up to an hour to show on Discord's side.
#
# Set restoreDefaults to true to reset everything back to defaults.
# It will automatically reset itself after.
# ============================================================

restoreDefaults: false

# ============================================================
# Default values for every command in the bot.
# These apply unless overridden by a category or command entry.
# ============================================================
defaults:
  isEnabled: true
  isAdminOnly: false
  isSlashEnabled: true
  isPrefixEnabled: true

# ============================================================
# Category-level overrides.
# Category keys match the folder names under src/commands
# (case-insensitive, spaces become underscores).
#
# isEnabled: false  -> disables ALL commands in that category
#                     regardless of individual command settings.
# isEnabled: true   -> allows individual commands to be toggled
#                     independently (the default).
#
# The other three keys (isSlashEnabled, isPrefixEnabled,
# isAdminOnly) act as the per-category default and CAN be
# overridden per-command.
# ============================================================
categories:
  # ------------------------------------------------
  # Birthday
  # ------------------------------------------------
  # birthday:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Community
  # ------------------------------------------------
  # community:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Core  (protected - cannot be disabled)
  # ------------------------------------------------
  # core:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Economy
  # ------------------------------------------------
  # economy:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Fun
  # ------------------------------------------------
  # fun:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Giveaway
  # ------------------------------------------------
  # giveaway:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # JoinToCreate
  # ------------------------------------------------
  # jointocreate:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Leveling
  # ------------------------------------------------
  # leveling:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Logging
  # ------------------------------------------------
  # logging:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Moderation
  # ------------------------------------------------
  # moderation:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Music
  # ------------------------------------------------
  # music:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Reaction_roles
  # ------------------------------------------------
  # reaction_roles:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Search
  # ------------------------------------------------
  # search:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # ServerStats
  # ------------------------------------------------
  # serverstats:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Starboard  (prefix-only commands)
  # ------------------------------------------------
  # starboard:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: false
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Ticket
  # ------------------------------------------------
  # ticket:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Tools
  # ------------------------------------------------
  # tools:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Utility
  # ------------------------------------------------
  # utility:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Verification
  # ------------------------------------------------
  # verification:
  #   isEnabled: true
  #   isAdminOnly: false
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

  # ------------------------------------------------
  # Welcome
  # ------------------------------------------------
  # welcome:
  #   isEnabled: true
  #   isAdminOnly: true
  #   isSlashEnabled: true
  #   isPrefixEnabled: true

# ============================================================
# Individual command overrides.
# Command names are lowercase, matching the file/slash name.
# Subcommands cannot be toggled individually; toggling the
# parent command affects all its subcommands.
#
# Set any key to override the category/default value.
# Only list keys you want to change.
#
# isAdminOnly     = require Manage Server permission
# isSlashEnabled  = register as a slash command
# isPrefixEnabled = usable via text prefix
# isEnabled       = load the command at all
#
# (Structurally prefix-only commands ignore isSlashEnabled.
#  Structurally slash-only commands ignore isPrefixEnabled.)
# ============================================================
commands:

  # ========================
  # Birthday
  # ========================
  birthday:
    # subcommands: set, info, list, remove, next, setchannel
    # isAdminOnly: true

  # ========================
  # Community
  # ========================
  apply:
    # subcommands: submit, status, list
  app-admin:
    isAdminOnly: true
    # subcommands: setup, review, list, dashboard

  # ========================
  # Core  (protected - cannot be disabled)
  # ========================
  # help - slash-only by code; isPrefixEnabled is ignored
  help:
    isSlashEnabled: true
    isPrefixEnabled: false
  # commands - protected; isAdminOnly by code
  commands:
    isAdminOnly: true
    # subcommands: dashboard, disable, enable
  # configwizard - slash-only by code; protected; isAdminOnly by code
  configwizard:
    isAdminOnly: true
    isSlashEnabled: true
    isPrefixEnabled: false
  # ping, stats, uptime, support are normal commands
  ping:
    # isAdminOnly: true
  stats:
    # isAdminOnly: true
  uptime:
    # isAdminOnly: true
  support:
    # isAdminOnly: true

  # ========================
  # Economy
  # ========================
  balance:
    # isAdminOnly: true
  beg:
    # isAdminOnly: true
  buy:
    # isAdminOnly: true
  crime:
    # isAdminOnly: true
  daily:
    # isAdminOnly: true
  deposit:
    # isAdminOnly: true
  economy:
    isAdminOnly: true
    isSlashEnabled: true
    isPrefixEnabled: false
    # subcommand: dashboard
  eleaderboard:
    # isAdminOnly: true
  fish:
    # isAdminOnly: true
  gamble:
    # isAdminOnly: true
  inventory:
    # isAdminOnly: true
  mine:
    # isAdminOnly: true
  pay:
    # isAdminOnly: true
  rob:
    # isAdminOnly: true
  shop:
    isSlashEnabled: true
    isPrefixEnabled: false
    # isAdminOnly: true
  shop-config:
    isSlashEnabled: true
    isPrefixEnabled: false
    isAdminOnly: true
    # subcommand: setrole
  slut:
    # isAdminOnly: true
  withdraw:
    # isAdminOnly: true
  work:
    # isAdminOnly: true

  # ========================
  # Fun
  # ========================
  # catfact - prefix-only by code; isSlashEnabled is ignored
  catfact:
    isSlashEnabled: false
    isPrefixEnabled: true
  count:
    isAdminOnly: true
    # subcommands: setup, disable, status, reset, leaderboard
  # dogfact - prefix-only by code; isSlashEnabled is ignored
  dogfact:
    isSlashEnabled: false
    isPrefixEnabled: true
  # fact - prefix-only by code; isSlashEnabled is ignored
  fact:
    isSlashEnabled: false
    isPrefixEnabled: true
  fight:
    # isAdminOnly: true
  flip:
    # isAdminOnly: true
  # react - prefix-only by code; isSlashEnabled is ignored
  react:
    isSlashEnabled: false
    isPrefixEnabled: true
  roll:
    # isAdminOnly: true

  # ========================
  # Giveaway
  # ========================
  gcreate:
    # isAdminOnly: true
  gdelete:
    isAdminOnly: true
  gend:
    isAdminOnly: true
  greroll:
    isAdminOnly: true

  # ========================
  # JoinToCreate
  # ========================
  jointocreate:
    isAdminOnly: true
    # subcommands: setup, dashboard

  # ========================
  # Leveling
  # ========================
  level:
    isAdminOnly: true
    # subcommands: setup, dashboard
  leveladd:
    isAdminOnly: true
  levelremove:
    isAdminOnly: true
  levelset:
    isAdminOnly: true
  leaderboard:
    # isAdminOnly: true
  rank:
    # isAdminOnly: true

  # ========================
  # Logging
  # ========================
  logging:
    isAdminOnly: true
    # subcommands: dashboard, channel

  # ========================
  # Moderation
  # ========================
  ban:
    isAdminOnly: true
  cases:
    isAdminOnly: true
  dm:
    isAdminOnly: true
  kick:
    isAdminOnly: true
  lock:
    isAdminOnly: true
  massban:
    isAdminOnly: true
  masskick:
    isAdminOnly: true
  purge:
    isAdminOnly: true
  say:
    isAdminOnly: true
  timeout:
    isAdminOnly: true
  unban:
    isAdminOnly: true
  unlock:
    isAdminOnly: true
  untimeout:
    isAdminOnly: true
  usernotes:
    isAdminOnly: true
    # subcommands: add, view, remove, clear
  warn:
    isAdminOnly: true
  warnings:
    isAdminOnly: true

  # ========================
  # Music
  # ========================
  join:
    # isAdminOnly: true
  music:
    # subcommands: pause, resume, skip, stop, shuffle, loop,
    #              volume, seek, remove, move, clear, leave, 247,
    #              likes add, likes list, likes remove, likes play
    # isAdminOnly: true
  nowplaying:
    # isAdminOnly: true
  play:
    isSlashEnabled: true
    isPrefixEnabled: false
    # isAdminOnly: true
  queue:
    isSlashEnabled: true
    isPrefixEnabled: false
    # isAdminOnly: true

  # ========================
  # Reaction_roles
  # ========================
  reactroles:
    isAdminOnly: true
    # subcommands: setup, dashboard

  # ========================
  # Search
  # ========================
  search:
    # isAdminOnly: true
    # subcommands: define, google, urban

  # ========================
  # ServerStats
  # ========================
  serverstats:
    isAdminOnly: true
    # subcommands: create, list, update, delete

  # ========================
  # Starboard  (prefix-only commands)
  # ========================
  setchannelstarboard:
    isAdminOnly: true
    isSlashEnabled: false
    isPrefixEnabled: true
  removestarboard:
    isAdminOnly: true
    isSlashEnabled: false
    isPrefixEnabled: true

  # ========================
  # Ticket
  # ========================
  ticket:
    isAdminOnly: true
    # subcommands: setup, dashboard
  claim:
    # isAdminOnly: true
  close:
    # isAdminOnly: true
  priority:
    # isAdminOnly: true

  # ========================
  # Tools
  # ========================
  baseconvert:
    # isAdminOnly: true
  calculate:
    # isAdminOnly: true
  countdown:
    # isAdminOnly: true
  embedbuilder:
    # isAdminOnly: true
  generatepassword:
    # isAdminOnly: true
  hexcolor:
    # isAdminOnly: true
  poll:
    # isAdminOnly: true
  randomuser:
    # isAdminOnly: true
  shorten:
    # isAdminOnly: true
  time:
    # isAdminOnly: true
  unixtime:
    # isAdminOnly: true

  # ========================
  # Utility
  # ========================
  avatar:
    # isAdminOnly: true
  firstmsg:
    # isAdminOnly: true
  report:
    # subcommands: file, setchannel
    # isAdminOnly: true
  serverinfo:
    # isAdminOnly: true
  todo:
    # subcommands: add, list, complete, remove,
    #              share create, share add, share view,
    #              share addtask, share remove
    # isAdminOnly: true
  userinfo:
    # isAdminOnly: true
  weather:
    # isAdminOnly: true
  wipedata:
    isSlashEnabled: true
    isPrefixEnabled: false
    # isAdminOnly: true

  # ========================
  # Verification
  # ========================
  autoverify:
    isAdminOnly: true
    # subcommands: setup, dashboard
  verify:
    # isAdminOnly: true
  verification:
    isAdminOnly: true
    # subcommands: setup, remove, dashboard

  # ========================
  # Welcome
  # ========================
  autorole:
    isAdminOnly: true
    # subcommands: add, remove, list
  goodbye:
    isAdminOnly: true
    # subcommand: setup
  greet:
    isAdminOnly: true
    isSlashEnabled: true
    isPrefixEnabled: false
    # subcommand: dashboard
  welcome:
    isAdminOnly: true
    # subcommand: setup
